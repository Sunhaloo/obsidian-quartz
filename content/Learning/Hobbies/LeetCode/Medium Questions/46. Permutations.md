---
id: 46. Permutations
aliases: Using brute force and backtracking to find permutations of array / lists
tags:
  - backtracking
  - brute-force
  - leetcode
  - python
difficulty: medium
author: S.Sunhaloo
date: 2026-03-04
status: Completed
---

> [!TIP] Question
> - Question Link: https://leetcode.com/problems/permutations/

# My Answer

> [!INFO] Resource(s)
> - [[Python - String Permutations]]

> [!WARNING]
> Given that I use LeetCode to learn about Data Structures and Algorithms for my university.
> 
> If you go and take a look at the above 'Subset Generation' note. You are going to see that this is basically a '*copy-paste*'.

> [!NOTE]
> Here I am going to have to only use the [[Python - String Permutations#Backtracking | backtracking]] approach only because of the way that we have to *output* our result!

```python
def permute(self, nums: List[int]) -> List[List[int]]:
    # initialise result list
    result = []

    # check for the base cases ==> length of the strings
    if len(nums) == 1:
        # NOTE: in Python `data[:]` is the same as `data.copy()`
        # `list[:]` is slightly ( in terms of nanoseconds ) faster than `list.copy()`
        # again, as they shared same memory... need to work with copy
        return [nums[:]]

    # iterate through the length of the array
    for i in range(len(nums)):
        # remove the first element from the array
        n = nums.pop(0)

        # run the function again on the remaining data of the list
        perms = self.permute(nums)

        # add the element remove at the top, back to each sub-permutation
        for perm in perms:
            # add the remove element to the "individual" list
            perm.append(n)

        # add remaining "permutated" elements to our `result` list
        result.extend(perms)

        # backtrack; restore the list for the next iteration and move up the tree
        nums.append(n)

    return result
```

> [!INFO]
> I see that NeetCode made another video on that same LeetCode problem and in that video. He did show both a **recursive** and an **iterative** method.

---

# Solutions

> [!INFO] Resource(s)
> - Doocs Leetcode:
> 	- https://leetcode.doocs.org/en/lc/46
> - YouTube Video(s):
> 	- https://www.youtube.com/watch?v=s7AvT7cGdSo ( _I **did** watch this video_ )
> 	- https://www.youtube.com/watch?v=FZe0UqISmUw ( _I did **not** watch this video_ )

```python

```

---

# Socials

- **Instagram**: https://www.instagram.com/s.sunhaloo
- **YouTube**: https://www.youtube.com/@s.sunhaloo
- **GitHub**: https://www.github.com/Sunhaloo

---

S.Sunhaloo
Thank You!